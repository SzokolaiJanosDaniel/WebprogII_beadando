<!doctype html>
<html lang="hu">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= (typeof locals.title !== 'undefined' && locals.title) ? locals.title : 'Jani@Szabi Kft.' %></title>
  <link rel="stylesheet" href="/assets/css/style.css">
 
</head>
<body>
  <header class="site-header"><h1>Jani&Szabi Kft.</h1></header>
  <nav class="nav">
    <a href="/">Főldal</a>
    <a href="/munkalapok">Adatbázis</a>
    <a href="/kapcsolat">Kapcsolat</a>
    <a href="/szerelok">CRUD</a>
    <span class="nav-spacer"></span>
    <% if (currentUser) { %>
      <a href="/messages">Üzenetek</a>
      <% if (currentUser.role==='admin') { %><a href="/admin">Admin</a><% } %>
      <form action="/auth/logout" method="post" class="nav-logout">
        <button>Kijelentkezés</button>
      </form>
    <% } else { %>
      <a style="margin-left:auto" href="/auth/login">Bejelentkezés</a>
      <a href="/auth/register">Regisztráció</a>
    <% } %>
  </nav>
  <main class="container">
    <% if (success && success.length) { %><div class="flash success"><%= success %></div><% } %>
    <% if (error && error.length) { %><div class="flash error"><%= error %></div><% } %>
    <%- body %>
  </main>
</body>
</html>
